# Release v0.5.6 - Barras, Fechas, Impresión, Carteles y Códigos Autogenerados - Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Release v0.5.6: escaneo de códigos de barras funcional en Ventas y Compras, unificación de formato/hora de fechas (incluido el ticket de venta que hoy imprime UTC crudo), checkbox de impresión de ticket por venta, carteles de éxito/error visibles tras acciones, y códigos autogenerados F0001/F0002 para productos sin código de barras físico.

**Architecture:** Centralizar toda la lógica transversal en módulos reutilizables: `style.py` ganará helpers de carteles (flash) con `st.session_state`; se creará `fechas.py` con `formatear_fecha_hora()` que normaliza los dos formatos de fecha que hoy conviven (ISO local con `T` del adapter de Python, y `YYYY-MM-DD HH:MM:SS` UTC de `CURRENT_TIMESTAMP`) y los convierte a hora local con formato `%d/%m/%Y %H:%M` como el ticket de prueba; `database.py` ganará `proximo_codigo_fraccionado()` y helpers de búsqueda por código/nombre para el escaneo unificado.

**Tech Stack:** Python 3, Streamlit, sqlite3, pytest, hypothesis (ya en requirements).

## Global Constraints

- Siguiente versión: `0.5.6` (PATCH). Actualizar `APP_VERSION` en `updater.py:37` a `"0.5.6"` ANTES de releasear. Prohibido release sin entrada en `CHANGELOG.md`.
- Conventional Commits en español, tipo + scope (ej: `fix(ventas): ...`, `feat(productos): ...`, `test(fechas): ...`).
- Sin emojis en commits, CHANGELOG, código ni UI. Prohibido en textos nuevos de interfaz.
- CHANGELOG: formato Keep a Changelog en español, citar `archivo:función` cuando aplica, máximo ~800 palabras por release.
- No commitear: `lubricentro.db`, `impresora.log`, `__pycache__/`, `comprobantes/`, `dist/`, `venv/`.
- Todos los tests deben pasar antes de cada commit: `python -m pytest tests/ -x -q`.
- TDD obligatorio por tarea: escribir test primero, verlo fallar, implementar, verlo pasar.
- No re-arquitecturar nada fuera de lo listado. No dividir `updater.py` ni `database.py`.
- La hora mostrada al usuario SIEMPRE es hora local de la PC donde corre la app; el formato unificado es `%d/%m/%Y %H:%M`.
- `codigo_barras` de `productos` sigue `TEXT UNIQUE`; los códigos autogenerados usan prefijo `F` (nunca colisiona con EAN/UPC reales).
- El rol "dueño" = rol `admin` (usuario seed `admin`). El checkbox de impresión aplica a todas las ventas de todos los roles (decisión del dueño: por venta, default marcado).

---

### Task 1: Módulo `fechas.py` con `formatear_fecha_hora` y tests

**Files:**
- Create: `fechas.py`
- Test: `tests/test_fechas.py`

**Interfaces:**
- Consumes: nada.
- Produces: `formatear_fecha_hora(valor) -> str` — acepta `datetime` (se formatea directo) o `str`; si el string contiene `T` se interpreta como ISO local (formato del adapter `sqlite3.register_adapter(datetime, isoformat)` de `database.py:12`); si no, se interpreta como `YYYY-MM-DD HH:MM:SS` UTC (formato de `DEFAULT CURRENT_TIMESTAMP` de SQLite) y se convierte a hora local. Devuelve `""` para `None`/vacío; devuelve el string crudo si no puede parsearlo. Formato de salida: `%d/%m/%Y %H:%M`.

- [ ] **Step 1: Write the failing test**

Create `tests/test_fechas.py`:

```python
import os
import time
from datetime import datetime

import pytest

import fechas


def _set_tz(tz):
    os.environ["TZ"] = tz
    time.tzset()


needs_tzset = pytest.mark.skipif(
    not hasattr(time, "tzset"), reason="time.tzset() solo disponible en Unix"
)


def test_none_vacio_devuelve_vacio():
    assert fechas.formatear_fecha_hora(None) == ""
    assert fechas.formatear_fecha_hora("") == ""


def test_datetime_local_se_formatea():
    dt = datetime(2026, 8, 1, 10, 30, 0)
    assert fechas.formatear_fecha_hora(dt) == "01/08/2026 10:30"


def test_string_iso_con_T_se_formatea_directo():
    assert fechas.formatear_fecha_hora("2026-08-01T10:30:45.123456") == "01/08/2026 10:30"


@needs_tzset
def test_string_utc_sin_T_se_convierte_a_local():
    tz_original = os.environ.get("TZ")
    _set_tz("America/Argentina/Buenos_Aires")
    try:
        assert fechas.formatear_fecha_hora("2026-08-01 14:30:45") == "01/08/2026 11:30"
    finally:
        if tz_original:
            _set_tz(tz_original)
        else:
            os.environ.pop("TZ", None)


def test_string_invalido_devuelve_crudo():
    assert fechas.formatear_fecha_hora("no-es-fecha") == "no-es-fecha"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_fechas.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'fechas'`

- [ ] **Step 3: Write minimal implementation**

Create `fechas.py`:

```python
from datetime import datetime, timezone

FORMATO_SALIDA = "%d/%m/%Y %H:%M"
FORMATO_UTC = "%Y-%m-%d %H:%M:%S"


def formatear_fecha_hora(valor):
    """Normaliza una fecha (datetime o string de la DB) a hora local y la formatea.

    - datetime: se formatea directo.
    - str con 'T': ISO local del adapter de database.py, se parsea directo.
    - str sin 'T' (YYYY-MM-DD HH:MM:SS): UTC de CURRENT_TIMESTAMP de SQLite,
      se convierte a hora local (con version DST-aware del offset).
    Devuelve "" para None/vacio y el string crudo si no puede parsear.
    """
    if valor is None or valor == "":
        return ""
    if isinstance(valor, datetime):
        return valor.strftime(FORMATO_SALIDA)

    s = str(valor).strip()
    try:
        if "T" in s:
            dt = datetime.fromisoformat(s)
        else:
            dt = datetime.strptime(s, FORMATO_UTC)
            dt = dt.replace(tzinfo=timezone.utc).astimezone().replace(tzinfo=None)
    except ValueError:
        return s
    return dt.strftime(FORMATO_SALIDA)
```

- [ ] **Step 4: Run test to verify it passes**

Run: `python -m pytest tests/test_fechas.py -v`
Expected: PASS (5 passed)

- [ ] **Step 5: Commit**

```bash
git add fechas.py tests/test_fechas.py
git commit -m "feat(fechas): agrega formateador unificado de fecha y hora"
```

---

### Task 2: Tickets usan `formatear_fecha_hora` (prueba y venta con mismo formato)

**Files:**
- Modify: `tickets.py:18-78` (generar_ticket_texto), `tickets.py:81-133` (facturas), `tickets.py:221-237` (imprimir_prueba)
- Test: `tests/test_fechas.py`

**Interfaces:**
- Consumes: `fechas.formatear_fecha_hora` (Task 1).
- Produces: ticket de venta imprime `Fecha: 01/08/2026 10:30` (local) igual que el de prueba. El de prueba mantiene su comportamiento (usa `datetime.now()` → formato idéntico).

- [ ] **Step 1: Write the failing test**

Append to `tests/test_fechas.py`:

```python
import tickets


@needs_tzset
def test_ticket_venta_formatea_fecha_utc_a_local():
    tz_original = os.environ.get("TZ")
    _set_tz("America/Argentina/Buenos_Aires")
    try:
        venta = {
            "tipo_comprobante": "ticket",
            "punto_venta": "0001",
            "numero_comprobante": 42,
            "subtotal": 100.0,
            "iva": 0.0,
            "total": 100.0,
            "metodo_pago": "efectivo",
            "creado_en": "2026-08-01 14:30:45",
        }
        texto = tickets.generar_ticket_texto(venta, [], None)
        assert "Fecha: 01/08/2026 11:30" in texto
        assert "2026-08-01 14:30:45" not in texto
    finally:
        if tz_original:
            _set_tz(tz_original)
        else:
            os.environ.pop("TZ", None)
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_fechas.py::test_ticket_venta_formatea_fecha_utc_a_local -v`
Expected: FAIL — el ticket imprime `Fecha: 2026-08-01 14:30:45` crudo (contiene el string crudo que el assert prohibe).

- [ ] **Step 3: Write minimal implementation**

In `tickets.py`:
- Add import at top (junto a `from datetime import datetime`): `import fechas`
- Line 30, replace:
```python
    lineas.append(f"Fecha: {venta['creado_en']}")
```
with:
```python
    lineas.append(f"Fecha: {fechas.formatear_fecha_hora(venta['creado_en'])}")
```
- Line 93 (factura A), replace:
```python
    lineas.append(f"Fecha: {venta['creado_en']}")
```
with:
```python
    lineas.append(f"Fecha: {fechas.formatear_fecha_hora(venta['creado_en'])}")
```
- `imprimir_prueba` (line 230) ya usa `datetime.now().strftime('%d/%m/%Y %H:%M')` — reemplazar por el mismo helper para una sola fuente de verdad:
```python
    lineas.append(f"  Fecha: {fechas.formatear_fecha_hora(datetime.now())}")
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `python -m pytest tests/test_fechas.py -v`
Expected: PASS (6 passed)

- [ ] **Step 5: Commit**

```bash
git add tickets.py tests/test_fechas.py
git commit -m "fix(tickets): imprime fecha y hora en formato local unificado"
```

---

### Task 3: Helpers de búsqueda de producto por código/nombre en `database.py`

**Files:**
- Modify: `database.py` (cerca de `get_productos`, línea 960)
- Test: `tests/test_database.py`

**Interfaces:**
- Consumes: `get_connection` (database.py), `get_productos` (database.py:960).
- Produces:
  - `buscar_producto_por_codigo(codigo) -> tuple|None` — busca exacto (case-insensitive, trimmed) en `codigo_barras` entre productos activos. Devuelve la tupla completa de `get_productos` o None.
  - `buscar_productos_por_nombre(termino) -> list[tuple]` — LIKE `%termino%` case-insensitive sobre `nombre` entre activos, ordenado por nombre.

- [ ] **Step 1: Write the failing test**

Append to `tests/test_database.py`:

```python
def test_buscar_producto_por_codigo_happy_path(temp_db):
    cat_id, prov_id = _crear_dependencias()
    database.add_producto("7791001", "Aceite 20W50", "", cat_id, prov_id, "Entero", 1, 1, 2)
    database.add_producto("F0001", "Aceite suelto", "", cat_id, prov_id, "Fraccionable", 1, 1, 2)
    p = database.buscar_producto_por_codigo("f0001")
    assert p is not None
    assert p[2] == "Aceite suelto"


def test_buscar_producto_por_codigo_inexistente_devuelve_none(temp_db):
    cat_id, prov_id = _crear_dependencias()
    assert database.buscar_producto_por_codigo("9999999") is None


def test_buscar_productos_por_nombre_filtra_y_ordena(temp_db):
    cat_id, prov_id = _crear_dependencias()
    database.add_producto("1", "Aceite hidraulico 20L", "", cat_id, prov_id, "Entero", 1, 1, 2)
    database.add_producto("2", "Aceite suelto", "", cat_id, prov_id, "Fraccionable", 1, 1, 2)
    database.add_producto("3", "Filtro de aire", "", cat_id, prov_id, "Entero", 1, 1, 2)
    res = database.buscar_productos_por_nombre("aceite")
    assert len(res) == 2
    assert res[0][2] == "Aceite hidraulico 20L"


def test_buscar_productos_por_nombre_excluye_inactivos(temp_db):
    cat_id, prov_id = _crear_dependencias()
    database.add_producto("1", "Activo", "", cat_id, prov_id, "Entero", 1, 1, 2)
    database.add_producto("2", "Inactivo", "", cat_id, prov_id, "Entero", 1, 1, 2)
    conn = database.get_connection()
    conn.execute("UPDATE productos SET activo = 0 WHERE codigo_barras = '2'")
    conn.commit()
    conn.close()
    res = database.buscar_productos_por_nombre("vo")
    assert [p[2] for p in res] == ["Activo"]
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_database.py -k "buscar_producto" -v`
Expected: FAIL — `AttributeError: module 'database' has no attribute 'buscar_producto_por_codigo'`

- [ ] **Step 3: Write minimal implementation**

In `database.py`, right after `get_productos()` (line 970):

```python
def buscar_producto_por_codigo(codigo):
    """Busca un producto activo por codigo de barras exacto (sin distinguir mayusculas)."""
    if not codigo:
        return None
    codigo = str(codigo).strip().lower()
    conn = get_connection()
    try:
        row = conn.execute("""
            SELECT p.*, c.nombre as categoria_nombre, prov.nombre as proveedor_nombre
            FROM productos p
            LEFT JOIN categorias c ON p.categoria_id = c.id
            LEFT JOIN proveedores prov ON p.proveedor_id = prov.id
            WHERE p.activo = 1 AND LOWER(p.codigo_barras) = ?
        """, (codigo,)).fetchone()
        return row
    finally:
        conn.close()


def buscar_productos_por_nombre(termino):
    """Busca productos activos cuyo nombre contiene el termino (sin distinguir mayusculas)."""
    if not termino:
        return []
    conn = get_connection()
    try:
        return conn.execute("""
            SELECT p.*, c.nombre as categoria_nombre, prov.nombre as proveedor_nombre
            FROM productos p
            LEFT JOIN categorias c ON p.categoria_id = c.id
            LEFT JOIN proveedores prov ON p.proveedor_id = prov.id
            WHERE p.activo = 1 AND p.nombre LIKE ? COLLATE NOCASE
            ORDER BY p.nombre
        """, (f"%{termino.strip()}%",)).fetchall()
    finally:
        conn.close()
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `python -m pytest tests/test_database.py -k "buscar_producto" -v`
Expected: PASS (4 passed)

- [ ] **Step 5: Commit**

```bash
git add database.py tests/test_database.py
git commit -m "feat(productos): agrega busqueda por codigo de barras y por nombre"
```

---

### Task 4: Ventas — campo único de escaneo que agrega al carrito

**Files:**
- Modify: `database.py` (agregar `resolver_producto`)
- Modify: `pages/7_Ventas.py:139-184` (reemplaza campo info + selectbox)
- Test: `tests/test_database.py`

**Interfaces:**
- Consumes: `database.buscar_producto_por_codigo`, `database.buscar_productos_por_nombre` (Task 3), `st.session_state.venta_items` (estructura existente: `{producto_id, nombre, cantidad, precio, stock}`).
- Produces: `resolver_producto(termino) -> tuple|None` en database.py (código exacto primero, nombre exacto único después). En UI: un `st.text_input("Buscar producto")` unificado; escaneando un código o escribiendo un nombre completo se muestra el producto con botón "Agregar al carrito". El selectbox "Seleccionar producto" se ELIMINA.

- [ ] **Step 1: Write the failing test (lógica extraída)**

Append to `tests/test_database.py`:

```python
def test_resolver_producto_por_codigo_o_nombre(temp_db):
    cat_id, prov_id = _crear_dependencias()
    database.add_producto("7792001", "Aceite 20L", "", cat_id, prov_id, "Entero", 1, 1, 2)
    database.add_producto("F0001", "Aceite suelto", "", cat_id, prov_id, "Fraccionable", 1, 1, 2)
    by_code = database.resolver_producto("F0001")
    assert by_code is not None and by_code[2] == "Aceite suelto"
    by_name = database.resolver_producto("aceite 20l")
    assert by_name is not None and by_name[2] == "Aceite 20L"
    assert database.resolver_producto("zzz inexistente") is None
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_database.py::test_resolver_producto_por_codigo_o_nombre -v`
Expected: FAIL — `AttributeError: module 'database' has no attribute 'resolver_producto'`

- [ ] **Step 3: Implement `resolver_producto` en `database.py`**

```python
def resolver_producto(termino):
    """Resuelve un termino de busqueda: primero codigo de barras exacto, luego nombre exacto."""
    if not termino:
        return None
    p = buscar_producto_por_codigo(termino)
    if p:
        return p
    coincidencias = buscar_productos_por_nombre(termino)
    if len(coincidencias) == 1:
        return coincidencias[0]
    return None
```

Run: `python -m pytest tests/test_database.py::test_resolver_producto_por_codigo_o_nombre -v`
Expected: PASS

- [ ] **Step 4: Replace the barcode block in `pages/7_Ventas.py:139-184`**

Delete lines 139-184 (campo "Codigo de barras" informativo + selectbox "Seleccionar producto" + botón "Agregar al carrito" + preview de precio) and replace with:

```python
st.markdown("#### Productos")

# Busqueda unificada: escanear codigo de barras o escribir nombre
col_busq, col_agregar = st.columns([3, 1])
with col_busq:
    termino = st.text_input(
        "Buscar producto (escanee codigo de barras o escriba nombre)",
        placeholder="Escanee el codigo o escriba el nombre",
        key="venta_busqueda"
    )
with col_agregar:
    st.write("")
    agregar_click = st.button("Agregar al carrito", use_container_width=True, key="venta_agregar_btn")

if termino:
    producto_encontrado = db.resolver_producto(termino)
    if producto_encontrado:
        p = producto_encontrado
        st.info(f"**{p[2]}** | Stock: {p[7]:.0f} | Precio: ${float(p[10]):.2f} | Cod: {p[1]}")
        if agregar_click:
            if p[7] <= 0:
                st.error("Producto sin stock disponible.")
            else:
                st.session_state.venta_items.append({
                    'producto_id': p[0],
                    'nombre': p[2],
                    'cantidad': 1.0,
                    'precio': float(p[10]),
                    'stock': float(p[7])
                })
                st.rerun()
    else:
        coincidencias = db.buscar_productos_por_nombre(termino)
        if coincidencias:
            st.warning(f"Hay {len(coincidencias)} productos con ese nombre. Escriba el nombre completo.")
        else:
            st.warning("Producto no encontrado con ese codigo o nombre.")
```

Note: cantidad siempre 1 al escanear; la cantidad se ajusta después en el carrito (las filas del form ya permiten editar cantidad con `vc_{idx}`).

- [ ] **Step 5: Run compile check + all tests**

Run: `python -m pytest tests/ -x -q`
Expected: PASS — no romper nada existente (el form del carrito y `crear_venta` no cambian).

- [ ] **Step 6: Commit**

```bash
git add database.py pages/7_Ventas.py tests/test_database.py
git commit -m "feat(ventas): escaneo unificado de codigo de barras que agrega al carrito"
```

---

### Task 5: Compras — campo de escaneo que rellena el producto de la fila

**Files:**
- Modify: `pages/8_Compras.py:46-92` (bloque de productos)

**Interfaces:**
- Consumes: `database.buscar_producto_por_codigo` (Task 3), `st.session_state.compra_items` (estructura existente: `[{producto, cantidad, precio}]`).
- Produces: un campo "Codigo de barras" sobre la lista de productos; al escanear y presionar el boton, rellena `compra_items[fila con producto None]` con el producto resuelto y autocompleta `precio` con el precio costo del producto.

- [ ] **Step 1: Implement el campo de escaneo en `pages/8_Compras.py`**

Insert after line 46 (`st.markdown("#### Productos")`):

```python
    col_scan, col_scan_info = st.columns([2, 2])
    with col_scan:
        codigo_scan = st.text_input(
            "Codigo de barras (escanear)",
            placeholder="Escane el codigo para rellenar el producto",
            key="compra_codigo_scan"
        )
    with col_scan_info:
        st.write("")
        if codigo_scan and st.button("Cargar en fila", key="compra_cargar_scan"):
            p = db.buscar_producto_por_codigo(codigo_scan)
            if p is None:
                st.error("No se encontro un producto con ese codigo de barras.")
            else:
                fila = next((i for i, it in enumerate(st.session_state.compra_items) if not it['producto']), None)
                if fila is None:
                    st.error("Todas las filas ya tienen producto. Quite una o agregue una fila.")
                else:
                    st.session_state.compra_items[fila]['producto'] = p[0]
                    st.session_state.compra_items[fila]['precio'] = float(p[9])
                    st.session_state.compra_codigo_scan = ""
                    st.rerun()
```

Note: el boton "Cargar en fila" es el disparador explicito; el text_input esta fuera de cualquier `st.form`, por lo que Enter no dispara el form de confirmar compra. `st.session_state.compra_codigo_scan = ""` limpia el campo.

- [ ] **Step 2: Run compile + tests**

Run: `python -m pytest tests/ -x -q`
Expected: PASS

- [ ] **Step 3: Commit**

```bash
git add pages/8_Compras.py
git commit -m "feat(compras): escaneo de codigo de barras rellena el producto de la fila"
```

---

### Task 6: Código autogenerado F0001/F0002 en `database.py` + botón "Generar código" en Productos

**Files:**
- Modify: `database.py` (junto a `add_producto`, línea 972)
- Modify: `pages/3_Productos.py:63-113` (form nuevo producto) y `pages/3_Productos.py:120-151` (edición)
- Test: `tests/test_database.py`

**Interfaces:**
- Consumes: `get_connection`.
- Produces: `proximo_codigo_fraccionado() -> str` — devuelve el siguiente código `F` libre (`F0001` si no hay ninguno; si existe `F0001`, devuelve `F0002`, etc.; formato: `F` + número con padding a 4 dígitos; si el número supera 9999, usa más dígitos sin truncar).

- [ ] **Step 1: Write the failing test**

Append to `tests/test_database.py`:

```python
def test_proximo_codigo_fraccionado_primero_es_F0001(temp_db):
    cat_id, prov_id = _crear_dependencias()
    assert database.proximo_codigo_fraccionado() == "F0001"


def test_proximo_codigo_fraccionado_incrementa(temp_db):
    cat_id, prov_id = _crear_dependencias()
    database.add_producto("F0001", "Aceite suelto", "", cat_id, prov_id, "Fraccionable", 1, 1, 2)
    database.add_producto("F0002", "Aceite suelto 2", "", cat_id, prov_id, "Fraccionable", 1, 1, 2)
    assert database.proximo_codigo_fraccionado() == "F0003"


def test_proximo_codigo_fraccionado_ignora_codigos_no_F(temp_db):
    cat_id, prov_id = _crear_dependencias()
    database.add_producto("7794001", "Aceite 20L", "", cat_id, prov_id, "Entero", 1, 1, 2)
    assert database.proximo_codigo_fraccionado() == "F0001"
```

- [ ] **Step 2: Run test to verify it fails**

Run: `python -m pytest tests/test_database.py -k "proximo_codigo_fraccionado" -v`
Expected: FAIL — `AttributeError`

- [ ] **Step 3: Write minimal implementation**

In `database.py`, right after `add_producto` (line 1011):

```python
def proximo_codigo_fraccionado():
    """Devuelve el siguiente codigo F-XXXX libre para productos sin codigo de barras fisico."""
    conn = get_connection()
    try:
        rows = conn.execute(
            "SELECT codigo_barras FROM productos WHERE codigo_barras LIKE 'F%'"
        ).fetchall()
        max_num = 0
        for (codigo,) in rows:
            if codigo is None:
                continue
            sufijo = codigo[1:]
            if sufijo.isdigit():
                max_num = max(max_num, int(sufijo))
        return f"F{max_num + 1:04d}"
    finally:
        conn.close()
```

- [ ] **Step 4: Run tests to verify they pass**

Run: `python -m pytest tests/test_database.py -k "proximo_codigo_fraccionado" -v`
Expected: PASS (3 passed)

- [ ] **Step 5: Botón "Generar código" en `pages/3_Productos.py`**

In the scanner block (line 68-76), add a button after the scanner `st.text_input`:

```python
     if st.button("Generar codigo F (sin barras)", key="gen_codigo_f"):
         st.session_state.codigo_barras_scanner = db.proximo_codigo_fraccionado()
         st.rerun()
```

El campo "Código de Barras" del form (`pages/3_Productos.py:85`, `disabled=True`) ya toma `value=codigo_barras` del session_state del scanner, así que el código generado aparece en el formulario antes de guardar. `add_producto` lo persiste tal cual.

En la edición (expander, líneas 120-151), el campo `new_cod_bar` ya es editable (`pages/3_Productos.py:127`); agregar un botón junto a él que rellena el campo via session_state:

```python
            if st.button("Generar F", key=f"gen_f_{pid}"):
                st.session_state[f"cb_{pid}"] = db.proximo_codigo_fraccionado()
                st.rerun()
```

- [ ] **Step 6: Run compile + all tests**

Run: `python -m pytest tests/ -x -q`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add database.py pages/3_Productos.py tests/test_database.py
git commit -m "feat(productos): genera codigo F-XXXX autoincremental para productos sin barras"
```

---

### Task 7: Carteles persistentes — helpers flash en `style.py` + migración de páginas

**Files:**
- Modify: `style.py`
- Modify: `pages/7_Ventas.py`, `pages/8_Compras.py`, `pages/1_Stock.py`, `pages/3_Productos.py`, `pages/2_Cuenta_Corriente.py`, `pages/8_Caja.py`, `pages/0_Gestion.py`

**Interfaces:**
- Produces (en `style.py`):
  - `flash_exito(msg)` — guarda `st.session_state["flash"] = ("success", msg)` y llama `st.toast(msg)`.
  - `flash_error(msg)` — guarda `st.session_state["flash"] = ("error", msg)`.
  - `mostrar_flash()` — renderiza `st.success`/`st.error` desde `st.session_state.flash` y limpia el estado. Se llama UNA vez al inicio de cada página mutadora (después de `inject_global_css()`).

**Patrón de migración por acción mutadora (mixto):**
- ÉXITO: reemplazar `st.success(...)` dentro del form por `flash_exito(...)`.
- ERROR: reemplazar `st.error(...)` dentro del form por `flash_error(...)`. Para errores de validación del form (campos obligatorios), también usar `flash_error` para que el mensaje persista tras el rerun y el usuario pueda corregir.
- Los `st.success`/`st.error` que ya están FUERA de form (botones simples de editar/eliminar) se dejan como están salvo que el bloque no haga `st.rerun()`; en ese caso se migran también para consistencia.
- Quitar emojis (✅❌⚠️ℹ️) de todos los textos migrados.

- [ ] **Step 1: Implement helpers en `style.py`**

Append to `style.py`:

```python
def flash_exito(mensaje):
    """Guarda un mensaje de exito para mostrar en el proximo render."""
    st.session_state["flash"] = ("success", mensaje)
    st.toast(mensaje)


def flash_error(mensaje):
    """Guarda un mensaje de error para mostrar en el proximo render."""
    st.session_state["flash"] = ("error", mensaje)


def mostrar_flash():
    """Muestra el mensaje flash pendiente (exito/error) y lo limpia."""
    if "flash" in st.session_state and st.session_state["flash"]:
        tipo, mensaje = st.session_state["flash"]
        if tipo == "success":
            st.success(mensaje)
        elif tipo == "error":
            st.error(mensaje)
        st.session_state["flash"] = None
```

- [ ] **Step 2: Migrar `pages/8_Caja.py`** (3 acciones: ajuste manual L44-71, cerrar caja L77-100, abrir caja L105-126)

- Add after `inject_global_css()`: `mostrar_flash()` (importar `from style import inject_global_css, mostrar_flash, flash_exito, flash_error`).
- L68 `st.success(...)` → `flash_exito(...)`; L51/L71 `st.error(...)` → `flash_error(...)`.
- L97 → `flash_exito(...)`; L100 → `flash_error(...)`.
- L123 → `flash_exito(...)`; L116/L126 → `flash_error(...)`.
- Mantener los `st.rerun()` existentes. Quitar emojis de los textos.

- [ ] **Step 3: Migrar `pages/7_Ventas.py`** (Confirmar Venta L209-271; Reintentar L283-292 queda fuera)

- `mostrar_flash()` tras `inject_global_css()` (L11).
- L259 `st.success(f"✅ Venta confirmada! {etiqueta}")` → `flash_exito(f"Venta confirmada {etiqueta}")` (sin emoji).
- Errores de validación L219, L224, L236, L244, L248, L271 → `flash_error(...)` (quitar emojis).
- El bloque de impresión (L262-267) queda para Task 8.
- NOTA: `flash_exito` lanza toast; tras `st.rerun()` el `mostrar_flash()` muestra también el cartel en línea (efecto mixto deseado).

- [ ] **Step 4: Migrar `pages/8_Compras.py`** (Confirmar Compra L97-119)

- `mostrar_flash()` tras `inject_global_css()`.
- L115 `st.success(...)` → `flash_exito(f"Compra #{compra_id} registrada correctamente.")`; L108/L119 → `flash_error(...)`.
- Anular compra (L144-150, fuera de form): mantener `st.success`/`st.error` + `st.rerun()` como está (ya visible).

- [ ] **Step 5: Migrar `pages/1_Stock.py`** (Ajuste L170-191; Aumento General L240-252; Aumento Parcial L270-284)

- `mostrar_flash()` tras `inject_global_css()`.
- L188 ajuste éxito → `flash_exito(...)`; L177/L191 → `flash_error(...)`.
- L249 → `flash_exito(...)`; L242/L252 → `flash_error(...)`.
- L281 → `flash_exito(...)`; L272/L274/L284 → `flash_error(...)`.

- [ ] **Step 6: Migrar `pages/3_Productos.py`** (Agregar Categoría L31-39; Guardar Producto L101-112; Desactivar L145-151)

- `mostrar_flash()` tras `inject_global_css()`.
- L36 → `flash_exito(...)`; L39 → `flash_error(...)`.
- L109 → `flash_exito(...)`; L104/L112 → `flash_error(...)`.
- Desactivar producto (L145-151): validar que la fila afectada sea 1; si no, `flash_error("No se pudo desactivar el producto.")`.
- Los edit/delete fuera de form (L47-59, L137-151) ya hacen rerun y están visibles; migrar por consistencia quitando emojis.

- [ ] **Step 7: Migrar `pages/2_Cuenta_Corriente.py`** (Registrar pago L126-156)

- `mostrar_flash()` tras `inject_global_css()`.
- L153 → `flash_exito(...)`; L143/L156 → `flash_error(...)`.

- [ ] **Step 8: Migrar `pages/0_Gestion.py`** (4 creaciones dentro de form: Cliente L21-37, Vehículo L92-113, Proveedor L164-179, Servicio L269-281)

- `mostrar_flash()` tras `inject_global_css()`.
- Migrar los 4 bloques con el mismo patrón (éxito→`flash_exito`, error→`flash_error`, quitar emojis).
- Los botones fuera de form (editar/eliminar/reactivar/aumentos/imprimir) ya son visibles; migrar por consistencia quitando emojis.

- [ ] **Step 9: Verificar sin emojis en archivos migrados**

Run: `grep -rn '✅\|❌\|⚠️\|ℹ️' pages/ | wc -l`
Expected: 0 en los archivos migrados (7_Ventas, 8_Compras, 1_Stock, 3_Productos, 2_Cuenta_Corriente, 8_Caja, 0_Gestion).

- [ ] **Step 10: Run all tests + compile check**

Run: `python -m pytest tests/ -x -q`
Expected: PASS

- [ ] **Step 11: Commit**

```bash
git add style.py pages/
git commit -m "fix(ui): carteles de exito y error visibles tras cada accion (flash persistente)"
```

**Fix 7A (revisor, Important): `flash_error` debe gatillar el rerun.**

Problema: `flash_error` solo guarda el mensaje, pero `mostrar_flash()` ya corrió al inicio del run actual; el cartel de error aparece recién en la SIGUIENTE interacción. Fix: `flash_error` llama `st.rerun()` tras guardar (el mensaje se consume en el run siguiente; sin loop). `flash_exito` NO cambia (ya muestra toast y los éxitos hacen su propio rerun; un rerun inmediato saltaría el bloque de impresión).

Nuevo `flash_error` en `style.py`:

```python
def flash_error(mensaje):
    """Guarda un mensaje de error y fuerza el rerun para mostrarlo."""
    st.session_state["flash"] = ("error", mensaje)
    st.rerun()
```

Verificar: ningún path existente donde `flash_error` se llame más de una vez por run (el rerun corta la ejecución, lo cual es el comportamiento deseado para ramas de error).

**Fix 7B (revisor, Important): convertir `st.warning` de ramas de operación mutadora a `flash_error` sin emojis.**

- `pages/0_Gestion.py:306` `st.warning("⚠️ No se actualizo ningun producto (verifique categoria/proveedor)")` → `flash_error("No se actualizo ningun producto (verifique categoria/proveedor)")`
- `pages/1_Stock.py:440` `st.warning("⚠️ El stock no cambió.")` → `flash_error("El stock no cambio.")` (sin tilde, consistente con textos sin acentos del proyecto)
- Los `st.warning`/`st.info` que son listas vacías / captions / avisos informativos NO se tocan (quedan inline).

Commit del fix:

```bash
git commit -m "fix(ui): carteles de error visibles de inmediato tras acciones fallidas"
```

---

### Task 8: Checkbox "Imprimir ticket" en Ventas

**Files:**
- Modify: `pages/7_Ventas.py` (form de venta L187-209 y bloque de impresión L262-267)

**Interfaces:**
- Consumes: `st.form("venta_form")` y `st.session_state.imprimir_ultima`.
- Produces: checkbox `imprimir_ticket` (default True) DENTRO del form de venta; si está desmarcado, NO se llama `imprimir_venta` y no se setea `imprimir_ultima`.

- [ ] **Step 1: Implementar el checkbox**

En el bloque `with st.form("venta_form")` (antes del `st.form_submit_button("Confirmar Venta")`, línea 209), agregar:

```python
        imprimir_ticket = st.checkbox("Imprimir ticket", value=True, key="venta_imprimir")
```

En el bloque de éxito (líneas 257-269), reemplazar el bloque de impresión por:

```python
                if venta_id:
                    etiqueta = f"{tipo_comp.upper()} {numero:08d}" if numero else f"#{venta_id}"
                    flash_exito(f"Venta confirmada {etiqueta}")
                    st.session_state.venta_items = []

                    if imprimir_ticket:
                        ok_print = imprimir_venta(venta_id, tipo_comp, cliente_id)
                        if ok_print:
                            st.info("Comprobante enviado a la impresora.")
                        else:
                            st.warning("No se pudo imprimir automaticamente. Boton de reintento abajo.")
                            st.session_state.imprimir_ultima = venta_id

                    st.rerun()
```

NOTA: este bloque fue migrado a flash en Task 7 Step 3; al aplicar esta Task ajustar el texto final SIN emoji y manteniendo `flash_exito`. Si `imprimir_ticket` es False, el ticket NO se imprime ni se guarda en `imprimir_ultima`.

- [ ] **Step 2: Verificar que `imprimir_ultima` no se setea si no se imprime**

El botón "Reintentar impresión" (L283-292) solo aparece si `imprimir_ultima` está seteado; con el checkbox off nunca se setea. No requiere cambio.

- [ ] **Step 3: Run tests**

Run: `python -m pytest tests/ -x -q`
Expected: PASS (la UI no tiene tests; verificar compilación con `python -m py_compile pages/7_Ventas.py`)

- [ ] **Step 4: Commit**

```bash
git add pages/7_Ventas.py
git commit -m "feat(ventas): agrega checkbox para imprimir o no el ticket por venta"
```

---

### Task 9: Unificar fechas visibles en páginas (Ventas, Compras, CC, Stock, Caja, Ordenes)

**Files:**
- Modify: `pages/7_Ventas.py:324`, `pages/8_Compras.py:131`, `pages/2_Cuenta_Corriente.py:43,170`, `pages/1_Stock.py:134,214`, `pages/8_Caja.py:35,170`, `pages/4_Ordenes.py:265`

**Interfaces:**
- Consumes: `fechas.formatear_fecha_hora` (Task 1).
- Produces: toda fecha mostrada en UI en formato local `%d/%m/%Y %H:%M`.

- [ ] **Step 1: `pages/7_Ventas.py:324`**

Replace:
```python
                st.write(f"**Fecha:** {v[10]}")
```
with:
```python
                st.write(f"**Fecha:** {fechas.formatear_fecha_hora(v[10])}")
```
Add import: `import fechas`.

- [ ] **Step 2: `pages/8_Compras.py:131`**

Replace:
```python
            st.write(f"**Fecha:** {c[3]}")
```
with:
```python
            st.write(f"**Fecha:** {fechas.formatear_fecha_hora(c[3])}")
```
Add import: `import fechas`.

- [ ] **Step 3: `pages/2_Cuenta_Corriente.py:43,170`**

Los `hasattr(..., 'strftime')` actuales reciben strings de la DB y fallan al branch. Reemplazar el formateo por `fechas.formatear_fecha_hora(valor)` en ambos lugares (la función maneja datetime y str).

- [ ] **Step 4: `pages/1_Stock.py:134,214`, `pages/8_Caja.py:35,170`, `pages/4_Ordenes.py:265`**

Mismo reemplazo: usar `fechas.formatear_fecha_hora(...)` en cada formateo de fecha visible.

- [ ] **Step 5: Verificación**

Run: `grep -rn "strftime" pages/` — confirmar que solo queden usos de `strftime` de los `date_input` para filtros (ej. `pages/7_Ventas.py:309-310`) que son correctos (fechas de filtro en UTC-normalizadas por SQL `date()`).
Run: `python -m pytest tests/ -x -q`
Expected: PASS

- [ ] **Step 6: Commit**

```bash
git add pages/ fechas.py
git commit -m "fix(ui): muestra todas las fechas en formato local unificado"
```

**Fix 9A (revisor, Important): fecha cruda en el expander de Compras.**

Problema: `pages/8_Compras.py:147` — el header del expander muestra la fecha cruda: `st.expander(f"#{c[0]} - {c[2]} - ${c[4]:.2f} - {c[3]} - {estado_label}")`. `c[3]` es la misma fecha de compra que se formatea dos líneas abajo. Fix: usar `fechas.formatear_fecha_hora(c[3])` en el título del expander (la página ya importa `fechas`).

```bash
git commit -m "fix(ui): fecha de compra en formato local en el expander"
```

---

### Task 10: CHANGELOG v0.5.6 y APP_VERSION

**Files:**
- Modify: `CHANGELOG.md`
- Modify: `updater.py:37`

- [ ] **Step 1: Actualizar `APP_VERSION`**

In `updater.py:37`, replace `APP_VERSION = "0.5.5"` with `APP_VERSION = "0.5.6"`.

- [ ] **Step 2: Agregar entrada CHANGELOG**

Add at the top of `CHANGELOG.md` (después del encabezado, antes de `## [0.5.5]`):

```markdown
## [0.5.6] - 2026-08-01

### Agregado
- Ventas: campo unificado de busqueda que resuelve codigo de barras escaneado o nombre y agrega el producto al carrito con un boton (`pages/7_Ventas.py`, `database.py:resolver_producto`)
- Compras: campo de escaneo de codigo de barras que rellena el producto de la fila y autocompleta el precio costo (`pages/8_Compras.py`)
- Productos: boton "Generar codigo F" que asigna un codigo autoincremental F0001, F0002... a productos sin codigo de barras fisico, evitando colisiones con codigos EAN/UPC reales (`database.py:proximo_codigo_fraccionado`)
- Ventas: checkbox "Imprimir ticket" por venta (marcado por defecto) para no imprimir cuando el dueño no lo desea (`pages/7_Ventas.py`)
- Busqueda de productos por codigo de barras y por nombre en database (`database.py:buscar_producto_por_codigo`, `database.py:buscar_productos_por_nombre`)

### Corregido
- Tickets: el ticket de venta imprimia la fecha cruda UTC de la DB con formato distinto al de prueba; ahora todas las fechas se normalizan a hora local con formato DD/MM/AAAA HH:MM como el ticket de prueba (`tickets.py`, `fechas.py`)
- Fechas: unificacion del formato de fecha y hora en Ventas, Compras, Cuenta Corriente, Stock, Caja y Ordenes (`fechas.py:formatear_fecha_hora`)
- Carteles: los mensajes de exito/error dentro de formularios desaparecian tras el submit de Streamlit; ahora se muestran de forma persistente (flash en session_state + toast para exitos) en todas las acciones mutadoras (`style.py`)
- Mensajes de la interfaz sin emojis

### Removido
- Ventas: campo informativo "Codigo de barras" que solo mostraba nombre y stock, y selectbox "Seleccionar producto" que no vinculaba el codigo escaneado (reemplazados por el campo unificado)
```

- [ ] **Step 3: Verificación final**

Run: `python -m pytest tests/ -x -q`
Expected: PASS
Run: `python -m py_compile app.py database.py tickets.py updater.py fechas.py lista_precios_pdf.py pages/*.py`
Expected: no errors
Run: `git status` — confirmar que no hay archivos no deseados (impresora.log, lubricentro.db, __pycache__).

- [ ] **Step 4: Commit**

```bash
git add CHANGELOG.md updater.py
git commit -m "chore(release): prepara v0.5.6"
```

- [ ] **Step 5: Tag y release (solo cuando el usuario lo pida)**

```bash
git tag v0.5.6
git push origin main --tags
```

El release en GitHub se crea con el ZIP de `build_windows.bat` adjunto (working tree limpio), replicando la sección [0.5.6] del CHANGELOG. NO hacer push/tag sin confirmación explícita del usuario.

---

### Fix post-review whole-branch (revisor final, todos Minor/baratos):

- **Fix WB-A (typo user-visible):** `pages/8_Compras.py:30` — placeholder `"Escane el codigo para rellenar el producto"` → `"Escanee el codigo para rellenar el producto"`.
- **Fix WB-B (tests F-code, Task 6):** agregar a `tests/test_database.py`:
  - Test 4+ digitos: con `F0009` y `F0010` en DB, `proximo_codigo_fraccionado()` → `"F0011"`.
  - Test prefijos F no numericos: con `F12X` y `FAB` en DB, `proximo_codigo_fraccionado()` → `"F0001"` (los ignora).
- **Fix WB-C (tests resolver_producto, Task 4):** agregar a `tests/test_database.py`:
  - `resolver_producto("")` → `None` (termino vacio).
  - Con dos productos de mismo nombre activos, `resolver_producto(nombre)` → `None` (coincidencia multiple).

Commit: `test(database): cubre rollover de codigos F y ramas de resolver_producto`

No tocar I1 (retry-impresion usa tipo_comp/cliente_id actuales en lugar de los de la venta almacenada, `pages/7_Ventas.py:280`) — pre-existente y fuera de scope del plan; queda registrado como follow-up.
