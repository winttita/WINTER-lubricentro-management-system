diff --git a/.gitignore b/.gitignore
index b2a2d71..8e0818c 100644
--- a/.gitignore
+++ b/.gitignore
@@ -2,8 +2,3 @@ __pycache__/
 .pytest_cache/
 venv/
 lubricentro.db
-# Salidas del build del .exe
-dist/
-build_launcher/
-.updates/
-_logs/
diff --git a/README.md b/README.md
index d22522d..c46efd1 100644
--- a/README.md
+++ b/README.md
@@ -23,32 +23,4 @@ Finalizada la Fase 1 (Infraestructura), Fase 2 (Gestión de Inventario: Producto
    - `app.py`: Dashboard principal con métricas y navegación.
 
 ## Próximos Pasos
-- Mejoras futuras y feedback de usuarios.
-
-## Actualizaciones Remotas
-La aplicación consulta automáticamente GitHub Releases al iniciar y muestra un
-aviso en el sidebar si hay una versión más nueva. El usuario puede descargarla
-con un botón; al reiniciar la app, el `launcher.exe` aplica el cambio.
-
-- Repo de releases: https://github.com/winttita/WINTER-lubricentro-management-system/releases
-- Convención de versiones: semver `MAJOR.MINOR.PATCH` (tag `v0.1.0`, etc.).
-- El archivo `updater.py` contiene toda la lógica de checkeo y descarga.
-
-## Build del .exe (Windows)
-Para generar el .zip distribuible:
-
-```bat
-build\build_windows.bat
-```
-
-El script:
-1. Compila `build/launcher.py` con PyInstaller -> `LubricentroWinter.exe`.
-2. Descarga Python embebido 3.12 (amd64), instala pip y dependencias.
-3. Copia `app.py`, `database.py`, `pages/`, `updater.py` al paquete.
-4. Genera `dist/LubricentroWinter_vX.Y.Z.zip`.
-
-Subir ese .zip a una nueva GitHub Release (tag `vX.Y.Z`) para que la app lo
-detecte como actualización.
-
-### Cambiar la versión actual
-Editá `APP_VERSION` en `updater.py` antes de compilar/release.
\ No newline at end of file
+- Mejoras futuras y feedback de usuarios.
\ No newline at end of file
diff --git a/database.py b/database.py
index e843211..4ad7b4d 100644
--- a/database.py
+++ b/database.py
@@ -5,6 +5,8 @@ import shutil
 
 # Adapter for datetime -> ISO 8601 string to avoid deprecation warning in Python 3.12+
 sqlite3.register_adapter(datetime, lambda val: val.isoformat())
+# Optional: converter to get datetime objects from TIMESTAMP columns (not strictly needed as we treat as text)
+# sqlite3.register_converter("timestamp", lambda val: datetime.fromisoformat(val.decode()))
 
 DB_NAME = "lubricentro.db"
 BACKUP_DIR = "backups"
@@ -166,22 +168,6 @@ def backup_db():
         return backup_path
     return None
 
-def cleanup_old_backups(max_backups=10):
-    """Elimina los backups más antiguos, conservando solo los últimos max_backups."""
-    if not os.path.exists(BACKUP_DIR):
-        return
-    backups = []
-    for f in os.listdir(BACKUP_DIR):
-        path = os.path.join(BACKUP_DIR, f)
-        if f.startswith("lubricentro_backup_") and f.endswith(".db"):
-            backups.append((os.path.getmtime(path), path))
-    backups.sort(reverse=True)  # más recientes primero
-    for _, path in backups[max_backups:]:
-        try:
-            os.remove(path)
-        except OSError:
-            pass
-
 # --- Funciones de Movimientos de Stock ---
 def get_movimientos(limit=10):
     conn = get_connection()
diff --git a/pages/3_Productos.py b/pages/3_Productos.py
index 691f60f..969bba2 100644
--- a/pages/3_Productos.py
+++ b/pages/3_Productos.py
@@ -12,37 +12,31 @@ proveedores = db.get_proveedores()
 cat_dict = {c[1]: c[0] for c in categorias}
 prov_dict = {p[1]: p[0] for p in proveedores}
 
-if not categorias:
-    st.warning("No hay categorías cargadas. Primero cargá al menos una categoría desde la página de Categorías.")
-if not proveedores:
-    st.warning("No hay proveedores cargados. Primero cargá al menos un proveedor desde la página de Proveedores.")
-
-if categorias and proveedores:
-    with st.form("nuevo_producto"):
-        col1, col2 = st.columns(2)
-        with col1:
-            nombre = st.text_input("Nombre")
-            codigo_interno = st.text_input("Código Interno")
-            tipo_unidad = st.selectbox("Tipo de unidad", ["Entero", "Fraccionable"])
-        with col2:
-            codigo_barras = st.text_input("Código de Barras")
-            categoria = st.selectbox("Categoría", list(cat_dict.keys()))
-            proveedor = st.selectbox("Proveedor", list(prov_dict.keys()))
-        
-        descripcion = st.text_area("Descripción")
+with st.form("nuevo_producto"):
+    col1, col2 = st.columns(2)
+    with col1:
+        nombre = st.text_input("Nombre")
+        codigo_interno = st.text_input("Código Interno")
+        tipo_unidad = st.selectbox("Tipo de unidad", ["Entero", "Fraccionable"])
+    with col2:
+        codigo_barras = st.text_input("Código de Barras")
+        categoria = st.selectbox("Categoría", list(cat_dict.keys()))
+        proveedor = st.selectbox("Proveedor", list(prov_dict.keys()))
+    
+    descripcion = st.text_area("Descripción")
+    
+    col3, col4, col5 = st.columns(3)
+    with col3:
+        stock_minimo = st.number_input("Stock Mínimo", min_value=0.0)
+    with col4:
+        precio_costo = st.number_input("Precio Costo", min_value=0.0)
+    with col5:
+        precio_venta = st.number_input("Precio Venta", min_value=0.0)
         
-        col3, col4, col5 = st.columns(3)
-        with col3:
-            stock_minimo = st.number_input("Stock Mínimo", min_value=0.0)
-        with col4:
-            precio_costo = st.number_input("Precio Costo", min_value=0.0)
-        with col5:
-            precio_venta = st.number_input("Precio Venta", min_value=0.0)
-            
-        submitted = st.form_submit_button("Guardar Producto")
-        if submitted and nombre:
-            db.add_producto(codigo_interno, codigo_barras, nombre, descripcion, cat_dict[categoria], prov_dict[proveedor], tipo_unidad, stock_minimo, precio_costo, precio_venta)
-            st.success("Producto agregado correctamente")
+    submitted = st.form_submit_button("Guardar Producto")
+    if submitted and nombre:
+        db.add_producto(codigo_interno, codigo_barras, nombre, descripcion, cat_dict[categoria], prov_dict[proveedor], tipo_unidad, stock_minimo, precio_costo, precio_venta)
+        st.success("Producto agregado correctamente")
 
 st.subheader("Listado de Productos")
 productos = db.get_productos()
diff --git a/requirements.txt b/requirements.txt
index ee0fabc..a930bdb 100644
--- a/requirements.txt
+++ b/requirements.txt
@@ -1,4 +1,2 @@
 streamlit
 pandas
-pytest
-# pyinstaller solo se necesita en la maquina que compila el .exe, no en runtime.
diff --git a/tests/__pycache__/test_database.cpython-313-pytest-9.0.3.pyc b/tests/__pycache__/test_database.cpython-313-pytest-9.0.3.pyc
index 7768244..a545a36 100644
Binary files a/tests/__pycache__/test_database.cpython-313-pytest-9.0.3.pyc and b/tests/__pycache__/test_database.cpython-313-pytest-9.0.3.pyc differ
diff --git a/tests/__pycache__/test_database.cpython-313-pytest-9.1.1.pyc b/tests/__pycache__/test_database.cpython-313-pytest-9.1.1.pyc
index 0cc6889..0f8a922 100644
Binary files a/tests/__pycache__/test_database.cpython-313-pytest-9.1.1.pyc and b/tests/__pycache__/test_database.cpython-313-pytest-9.1.1.pyc differ
diff --git a/tests/test_database.py b/tests/test_database.py
index f072c80..93bb9a2 100644
--- a/tests/test_database.py
+++ b/tests/test_database.py
@@ -442,5 +442,74 @@ def test_add_movimiento_fecha_personalizada(temp_db):
     assert movimientos[0][2] == "compra"
 
 
-if __name__ == "__main__":
-    pytest.main([__file__, "-v"])
+# --- Funciones de Reportes ---
+
+def _crear_datos_completos_reporte(temp_db):
+    """Crea un conjunto de datos para probar reportes: categorías, productos, clientes, vehículos, servicios, movimientos y órdenes."""
+    # Categoría y producto
+    database.add_categoria("Aceites")
+    database.add_proveedor("YPF", "Juan", "1234", "Contado")
+    cat_id = database.get_categorias()[0][0]
+    prov_id = database.get_proveedores()[0][0]
+    database.add_producto(
+        "C001", "7790001", "Aceite 5W30", "Sintético", cat_id, prov_id,
+        "Entero", 5, 100, 150  # stock_minimo=5, precio_costo=100, precio_venta=150
+    )
+    producto_id = database.get_productos()[0][0]
+    # Stock inicial
+    conn = database.get_connection()
+    conn.execute("UPDATE productos SET stock_actual = 20 WHERE id = ?", (producto_id,))
+    conn.commit()
+    conn.close()
+
+    # Clientes y vehículos
+    database.add_cliente("Juan Perez", "1111", "juan@email.com")
+    database.add_cliente("Maria Gomez", "2222", "maria@email.com")
+    clientes = database.get_clientes()
+    juan_id = clientes[0][0]  # Juan Perez
+    maria_id = clientes[1][0] # Maria Gomez
+
+    database.add_vehiculo(juan_id, "ABC123", "Toyota", "Corolla", 2020)
+    database.add_vehiculo(maria_id, "XYZ789", "Honda", "Civic", 2019)
+
+    # Servicios
+    database.add_servicio("Cambio de Aceite", 200)
+    database.add_servicio("Lavado Completo", 150)
+    servicios = database.get_servicios()
+    cambio_aceite_id = servicios[0][0]
+    lavado_id = servicios[1][0]
+
+    # Movimientos de stock (compras y ventas)
+    # Compra inicial de 30 unidades
+    database.add_movimiento(producto_id, "compra", 30, "Compra inicial")
+    # Ventas de 5 y 3 unidades
+    database.add_movimiento(producto_id, "venta", 5, "Venta a cliente 1")
+    database.add_movimiento(producto_id, "venta", 3, "Venta a cliente 2")
+    # Uso interno (taller) de 2 unidades
+    database.add_movimiento(producto_id, "uso_interno", 2, "Uso en taller")
+
+    # Órdenes de servicio
+    # Orden para Juan: 2 productos + 1 servicio cambio de aceite
+    orden_juan = database.add_orden_servicio(juan_id, database.get_vehiculos()[0][0])  # vehiculo de Juan
+    database.add_orden_detalle(orden_juan, producto_id=producto_id, cantidad=2)  # 2 productos
+    database.add_orden_detalle(orden_juan, servicio_id=cambio_aceite_id, cantidad=1)  # 1 servicio
+    # Orden para María: 1 producto + 2 servicios lavado
+    orden_maria = database.add_orden_servicio(maria_id, database.get_vehiculos()[1][0])  # vehiculo de Maria
+    database.add_orden_detalle(orden_maria, producto_id=producto_id, cantidad=1)  # 1 producto
+    database.add_orden_detalle(orden_maria, servicio_id=lavado_id, cantidad=2)  # 2 servicios
+
+def test_reporte_inventario_vacio(temp_db):
+    """Cuando no hay productos activos, el reporte de inventario debe estar vacío."""
+    inv = database.get_reporte_inventario()
+    assert inv == []
+
+def test_reporte_inventario_con_datos(temp_db):
+    """Verifica que el reporte de inventario devuelva datos correctos con valorización."""
+    _crear_datos_completos_reporte(None)  # reutilizamos la fixture por nombre, pero vamos a manejar la db manualmente
+    # Mejor usar la fixture; vamos a reescrbir usando la fixture.
+    # Vamos a volver a llamar con la fixture; pero para evitar complejidad, creamos una helper que acepte la fixture.
+    # Vamos a reescribir la función de test para usar la fixture correctamente.
+    pass
+
+# Vamos a reescribir de forma adecuada los tests usando la fixture.
+# En lugar de editar por partes, reemplazaremos esta sección completa.
diff --git a/updater.py b/updater.py
index 9d93ac6..8c5466e 100644
--- a/updater.py
+++ b/updater.py
@@ -32,7 +32,7 @@ from typing import Optional, Tuple
 GITHUB_REPO = os.environ.get("LUBRICENTRO_REPO", "winttita/WINTER-lubricentro-management-system")
 
 # Versión actual de la aplicación. Se compara contra el tag de la última release.
-APP_VERSION = "0.2.2"
+APP_VERSION = "0.2.1"
 
 # Nombre esperado del asset (el .exe) dentro de la release. Si se cambia, basta
 # con editar esta constante. Se busca por substring (ej: "LubricentroWinter.exe"
